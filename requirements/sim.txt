cocotb>=2.0.0
cocotb-test>=0.2.4
pytest>=7.0
numpy>=1.24
pyuvm>=4.0.1
pyvsc>=0.8.8
# Modern prebuilt z3 CLI for the formal (SymbiYosys) jobs -- see
# ci/sim.Dockerfile for why this replaces the distro apt z3 package.
z3-solver>=4.13
