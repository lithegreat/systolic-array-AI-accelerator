# Ruff configuration for the Group5 accelerator repository.
# The Didactic-SoC git submodule and the local `bin/` tools folder contain
# third-party / vendored code and binaries that we do not own or lint.
extend-exclude = [
    "Didactic-SoC",
    "bin",
    ".venv",
]
